# Stripe Basic Example

Minimal runnable Stripe charge example.

## Setup

1. Install dependencies:
   - `bun install`
2. Set environment variables:
   - `STRIPE_API_KEY`

## Run

```bash
STRIPE_API_KEY=sk_test_xxx bun run start
```
